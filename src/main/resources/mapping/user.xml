<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.demo.mapper.UserMapper">
	<resultMap type="com.example.demo.entity.User" id="map_user">
		<id property="userId" column="id"></id>
	</resultMap>

	<select id="findUserById" resultType="com.example.demo.entity.User">
		select * from `user` where id = #{arg0 }
	</select>
	
	<select id="findUserByUsername" resultType="com.example.demo.entity.User">
		select * from `user` where username = #{arg0 }
	</select>

	<insert id="addUser" parameterType="com.example.demo.entity.User" useGeneratedKeys="true" keyProperty="userId">
		insert into
			`user`
		(name, age, username, password)
		values
			(#{name }, #{age }, #{username }, #{password })
	</insert>
</mapper>